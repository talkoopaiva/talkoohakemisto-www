<li class="single-talkoot">
  <a href="{{url}}">
    <div class="img-talkoot">
      <img src="{{iconUrl}}" class="img-responsive" />
    </div>
    <div class="talkoot-info">
      <h3>Wikitalkoot</h3>
      <div class="talkoot-type">{{name}}</div>
      <div class="talkoot-desc">{{description}}</div>
      <div class="talkoot-meta">
        <i class="glyphicon glyphicon-map-marker"></i> {{street_address}}, {{municipality.name}}<br>
        <div class="hidden-xs"><i class="glyphicon glyphicon-user"></i> {{organizer}}</div>
      </div>
    </div>
    <div class="clear"></div>
  </a>
</li>
